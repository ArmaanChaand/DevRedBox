export default function ServicesSection() {
  return (
    <section className="section-wrapper space-y-5">
      <h2 className="text-2xl font-black text-muted-foreground">
        <span className="text-foreground">Our Services.</span> What we specialize in?
      </h2>
      <p className="text-lg max-w-xl">In one line, we build SaaS which runs on the web. Along with that we build chrome extensions if the SaaS requires it.</p>
      
    </section>
  );
}
